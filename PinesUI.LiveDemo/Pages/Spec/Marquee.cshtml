@page
@model PinesUI.LiveDemo.Pages.Spec.MarqueeModel

@{
    ViewData["Title"] = "Marquee Spec";
}

<h2>Marquee</h2>
<p class="text-neutral-500 mb-5">The <code class="code-cell">&lt;pines-marquee&gt;</code> component creates a horizontally scrolling container. It has no configurable parameters. Use child content to provide logos, images, or SVGs to scroll.</p>
