@{
    var columns = new List<(string Name, string DisplayName)>()
    {
        ("Id", "Id"),
        ("Name", "Name"),
        ("Message", "The Message that you sent")
    };


    var dataContent = new List<(string Id, string Name, string Message)>()
    {
        ("1", "Ting", "Hey what's up"),
        ("2", "Tong", "Fallen ballen"),
        ("3", "Tang", "I'm here!")
    };

    var x = (IList<object>)dataContent;

}

@*

<pines-table table-id="table" column-config="columns" data="x" display-action-col="true"></pines-table>

*@